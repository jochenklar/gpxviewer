gpxviewer
=========

A small Python script to visualize GPX files using [Leaflet](https://leafletjs.com/). It does only need Python 3 and no extra dependencies.

Usage
-----

```bash
./gpxviewer input.gpx
./gpxviewer input.gpx --template=different-template.html
```
